@model IEnumerable<WebApplication1.Models.StudentSubject>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Subject.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student.FirstName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Student.SecondName)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Subject.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Student.SecondName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.StudentSubjectId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.StudentSubjectId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.StudentSubjectId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
